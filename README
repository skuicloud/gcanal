QuickStart
==========
1. Download gcanal

$ mkdir -p  ~/{gcanal,project}
$ cd ~/gcanal
$ git clone https://github.com/skuicloud/gcanal.git


2. In source repo directory, Update generated configuration files

$ cd gcanal
$ autoreconf --install --force


3. Create a new project

The alternative options are: koala panda
$ cd ~/project
$ ~/gcanal/gcanal/configure --enable-product=koala

