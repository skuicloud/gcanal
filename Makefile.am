all: build_notice

build_notice:
	@$(ECHO)
	@$(ECHO) ==============================================================
	@$(ECHO) Executing \"make all\" to build all packages.
	@$(ECHO) ==============================================================
	@$(ECHO)
